你是“ML系统工程师 + 教学设计顾问”。  
请基于我“现有的 LLM fine-tune 项目”（模型、数据集、训练目标都已定，不要随意更换）输出一个可执行的迁移计划：把原项目从原有硬件环境迁移到 Google TPU，并最终交付一个可在 Google Colab 运行的 Jupyter Notebook。

数据量可以变小很多，最终目标是在GPU上跑一次的时间小于30分钟

项目背景与约束：
- 课程背景：商科转 Data Analysis 学生，编程与大模型基础有限。
- 教学目标：不是追求最强模型效果，而是让学生理解 TPU/GPU 在训练行为上的差异与工程权衡。
- 周期：可作为“一周课程单元”使用。
- 最终交付：1 个 Colab Notebook（可直接运行），支持在 GPU 和 TPU 各跑一次并记录对比结果。
- 我已有现成 fine-tune 流程，你需要“最小改动迁移”，优先保留原模型、原数据、原训练逻辑。


请严格按以下结构输出：

1) 目标重述（1段）
- 用一句话定义这个迁移项目的成功标准。

2) 前置检查清单
- 列出把现有脚本迁移到 TPU 前必须确认的兼容性项（框架版本、依赖、数据加载、动态 shape、precision、分布式策略等）。
- 每一项给“检查方法 + 通过标准 + 不通过时替代方案”。

3) 迁移路线（最小改动优先）
- 按阶段给出：Phase 0/1/2/3。
- 每阶段包含：输入、操作步骤、产出、风险、回滚策略。
- 明确哪些代码必须改、哪些代码尽量不改。

4) Colab Notebook 设计蓝图（按 Cell 编号）
- Cell 1~N：每个 Cell 写“目的、关键代码内容、预期输出”。
- 必须包含：环境安装、设备检测（CPU/GPU/TPU）、数据准备、模型加载、训练、评估、计时与日志、结果汇总可视化。
- 标注“学生可改参数区”和“禁止改动区”。

5) 实验设计（教学导向）
- 设计 GPU vs TPU 对比实验矩阵（至少 batch size 维度）。
- 指标至少包括：first-step overhead、steady-state throughput、total wall time、最终训练损失/任务指标。
- 给出结果表模板（列名清晰）和图表建议（至少2张）。

6) 教学可用性设计
- 给出“学生版任务说明”（低门槛、步骤化）。
- 给出“教师讲解要点”（5-8条，偏业务与系统直觉，不堆底层细节）。
- 给出“课堂常见报错与快速修复手册”（按症状->原因->修复命令）。

7) 风险与兜底方案
- 列出 TPU 迁移最常见的 8-12 个失败点。
- 每个失败点提供快速定位步骤。
- 提供“最坏情况 fallback”：若 TPU 当天不可用，如何用 GPU 保留教学目标。

8) 一周落地时间表
- Day 1~Day 5：每天的具体任务、完成定义（DoD）、产出物。

9) 最终交付清单
- 明确哪些文件必须提交（Notebook、Instructor Notes、results.csv、实验截图等）。
- 给出验收标准（可复现、可讲解、可比较）。

输出要求：
- 用中文。
- 尽量具体、可执行，不写空泛建议。
- 对每一步给出建议命令或代码片段（伪代码也可）。
- 若你认为我的现有流程有 TPU 不兼容点，请先“保留原方案前提下”给最小替代实现。
- 最后附一个“30分钟快速试跑版本”（MVP），用于我先验证整条链路通不通。